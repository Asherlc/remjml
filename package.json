{
  "name": "remjml",
  "private": true,
  "devDependencies": {
    "typescript": "^4.8.4"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "tsc --build --clean",
    "build": "tsc --build",
    "watch": "tsc --build --watch"
  }
}
